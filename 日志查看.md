# 查看 pm2 的完整日志（最关键）
pm2 logs silverquant-dashboard --lines 100

# 或者直接看 pm2 日志文件
cat ~/.pm2/logs/silverquant-dashboard-error.log | tail -100
cat ~/.pm2/logs/silverquant-dashboard-out.log | tail -100


# 查看最新的回测日志
ls -lt logs/backtest/ | head -5

# 查看最新日志的完整内容（将文件名替换为实际文件名）
cat logs/backtest/backtest_小盘股动态持有策略_bt_$(date +%Y%m%d).log

# 或者直接看今天所有回测日志的尾部
tail -100 logs/backtest/*$(date +%Y%m%d)*.log 2>/dev/null || cat logs/backtest/*.log | tail -100


python -c "
import traceback
try:
    from backtest.strategy_backtest.batch_backtest_portfolio import run_portfolio_backtest, get_stock_list_from_db, load_strategy_class
    stocks_df = get_stock_list_from_db(limit=3)
    stocks = stocks_df.to_dict('records')
    cls = load_strategy_class('小盘股动态持有策略_bt')
    result = run_portfolio_backtest(stocks=stocks, start_date='20260101', end_date='20260522', strategy_class=cls, initial_cash=100000)
    print('status:', result.get('status'))
    print('error:', result.get('error'))
    if result.get('traceback'):
        print(result['traceback'])
except Exception as e:
    traceback.print_exc()
" 2>&1
